const connectConfig = {
    ip: "localhost",
    port: "9000"
};

const urlConfig = {
    allMovies: "/v1/movie/all",
    allType: "/v1/movie/type",
    register: "/v1/user/register",
    login: "/v1/user/login"
};

const pageConfig = {
    pageSize: 18
};